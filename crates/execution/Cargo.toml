[package]
name = "execution"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
ingestion = { path = "../ingestion" }
async-trait = "0.1"
compact_str = "0.8"
anyhow = "1.0"
tracing = "0.1"

