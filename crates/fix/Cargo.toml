[package]
name = "fix"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { workspace = true, features = ["net", "io-util", "sync", "time"] }
tracing = { workspace = true }
chrono = { workspace = true }
